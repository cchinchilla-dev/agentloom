apiVersion: batch/v1
kind: CronJob
metadata:
  name: agentloom-scheduled
spec:
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: agentloom
              resources:
                requests:
                  memory: 64Mi
                  cpu: 50m
                limits:
                  memory: 128Mi
