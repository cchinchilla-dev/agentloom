apiVersion: batch/v1
kind: Job
metadata:
  name: agentloom-workflow
spec:
  template:
    spec:
      containers:
        - name: agentloom
          resources:
            requests:
              memory: 64Mi
              cpu: 50m
            limits:
              memory: 128Mi
