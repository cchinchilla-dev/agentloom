apiVersion: batch/v1
kind: Job
metadata:
  name: agentloom-workflow
spec:
  template:
    spec:
      containers:
        - name: agentloom
          resources:
            requests:
              memory: 256Mi
              cpu: 200m
            limits:
              memory: 512Mi
