apiVersion: batch/v1
kind: CronJob
metadata:
  name: agentloom-scheduled
spec:
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: agentloom
              resources:
                requests:
                  memory: 256Mi
                  cpu: 200m
                limits:
                  memory: 512Mi
